rules_version = '2';

// All reads/writes go through the backend (Admin SDK bypasses these rules).
// The browser client should never hit Firestore directly — only Firebase Auth.
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
